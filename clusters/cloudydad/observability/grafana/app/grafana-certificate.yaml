apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: grafana
spec:
  secretName: grafana-tls
  dnsNames:
    - 'grafana.cloudydad.world'
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
