---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: github-archiver-data
  namespace: gitea
  labels:
    app: github-archiver
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: truenas-iscsi-csi
  resources:
    requests:
      storage: 10Gi
